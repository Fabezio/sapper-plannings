const mongoose = require('mongoose')

const connection = mongoose.connect('mongodb://localhost/plannings', {
  useCreateIndex: true,
  useNewUrlParser: true,
  useUnifiedTopology: true
})
export { connection }
